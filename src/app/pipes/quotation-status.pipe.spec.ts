import { QuotationStatusPipe } from './quotation-status.pipe';

describe('QuotationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
