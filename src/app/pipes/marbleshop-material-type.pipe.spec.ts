import { MarbleshopMaterialTypePipe } from './marbleshop-material-type.pipe';

describe('MarbleshopMaterialTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MarbleshopMaterialTypePipe();
    expect(pipe).toBeTruthy();
  });
});
