import { BillCategoryPipe } from './bill-category.pipe';

describe('BillCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new BillCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
