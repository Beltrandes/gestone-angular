import { MiscellaneousMaterialTypePipe } from './miscellaneous-material-type.pipe';

describe('MiscellaneousMaterialTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MiscellaneousMaterialTypePipe();
    expect(pipe).toBeTruthy();
  });
});
